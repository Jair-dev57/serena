name: serena_backend
description: Backend de Serena — recomendación de ejercicios vía Claude.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  dart_frog: ^1.1.0
  dotenv: ^4.2.0
  http: ^1.2.0

dev_dependencies:
  test: ^1.24.0